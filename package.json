{
  "name": "@hyphen/react-sdk",
  "version": "0.5.0",
  "description": "Hyphen SDK for React",
  "type": "module",
  "main": "dist/index.cjs",
  "module": "dist/index.js",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "require": "./dist/index.cjs"
    }
  },
  "types": "dist/index.d.ts",
  "scripts": {
    "lint": "biome check --write --error-on-warnings",
    "test": "pnpm lint && vitest run --coverage",
    "test:ci": "biome check --error-on-warnings && vitest run --coverage",
    "build": "rimraf ./dist && tsup --format cjs,esm --dts",
    "clean": "rimraf ./dist pnpm-lock.yaml node_modules coverage",
    "prepare": "rimraf ./dist && tsup --format cjs,esm --dts"
  },
  "keywords": [
    "hyphen",
    "sdk",
    "browser",
    "react",
    "javascript",
    "typescript"
  ],
  "author": "Team Hyphen <hello@hyphen.ai>",
  "license": "MIT",
  "devDependencies": {
    "@biomejs/biome": "^2.3.5",
    "@faker-js/faker": "^10.1.0",
    "@swc/core": "^1.15.2",
    "@types/node": "^24.10.1",
    "@vitest/coverage-v8": "^4.0.9",
    "dotenv": "^17.2.3",
    "rimraf": "^6.1.0",
    "tsd": "^0.33.0",
    "tsup": "^8.5.1",
    "typescript": "^5.9.3",
    "vitest": "^4.0.9"
  },
  "files": [
    "dist",
    "LICENSE"
  ],
  "dependencies": {
    "@hyphen/browser-sdk": "^1.0.3",
    "hookified": "^1.13.0"
  }
}
